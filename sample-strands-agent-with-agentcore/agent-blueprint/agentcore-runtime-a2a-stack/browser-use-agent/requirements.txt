# Browser Use Agent Dependencies
# A2A Server for autonomous browser automation (NO Strands framework)

# Core Framework
fastapi==0.116.1
uvicorn[standard]==0.35.0
starlette>=0.32.0

# Browser Use - AI browser automation
# Using [aws] extra for AWS Bedrock support (includes boto3)
browser-use[aws]>=0.11.0

# A2A SDK for agent-to-agent communication (AgentCore Runtime contract)
a2a-sdk>=0.3.20

# AWS & Bedrock (included in browser-use[aws] but explicit for clarity)
boto3>=1.40.1
botocore>=1.34.0

# AgentCore Browser Client (for live view support)
bedrock-agentcore>=0.1.0

# HTTP
httpx>=0.24.0
anyio>=3.6.0

# OpenTelemetry (for observability)
aws-opentelemetry-distro>=0.10.1

# Pydantic (for data validation)
pydantic>=2.0.0
